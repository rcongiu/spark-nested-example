Creating nested data programmatically in spark
==============================================

code example to show how to load nested data 
(usually in parquet) starting from flat data.

Building
---------
sue sbt (sbt package)

Running
--------
You need to change the file locations in your code
but you can run the submit.sh script after you build the example.

